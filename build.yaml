---
name: "Broadcast"
guid: "a7e6f0d2-3c1e-4b7a-9f0a-1d2c3e4f5a6b"
version: "0.2.5.1"
targetAbi: "10.11.8.0"
framework: "net9.0"
overview: "Turns your Jellyfin library into a single always-on TV channel"
description: >
  Jellyfin Broadcast generates a continuous programming schedule from your
  media library and exposes it as a live channel with M3U/XMLTV support.
category: "General"
owner: "jellytv"
artifacts:
- "Jellyfin.Plugin.Broadcast.dll"
- "Microsoft.Data.Sqlite.dll"
- "SQLitePCLRaw.core.dll"
- "SQLitePCLRaw.batteries_v2.dll"
- "SQLitePCLRaw.provider.e_sqlite3.dll"
- "runtimes/**"
changelog: >
  Fix: config page was unstyled (raw white inputs, full-width flat button) -- the legacy
  emby-input/emby-button custom elements it used don't reliably pick up styling from Jellyfin's newer
  React/MUI dashboard shell. Rebuilt with self-contained CSS instead of depending on host CSS. Also
  includes 0.2.5.0's simplification: config page no longer exposes Channel settings or a Programming
  Block editor -- ships with a single default "All Day Movies" block that just works out of the box.
